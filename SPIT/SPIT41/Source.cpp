#include <iostream>
#include <algorithm>
#include <math.h>
using namespace std;

int main() {
    #ifndef ONLINE_JUDGE
    freopen("Input.inp", "r", stdin);
    freopen("Output.out", "w", stdout);
    #endif

    int A, B, C, k;
    cin >> A >> B >> C >> k;
    

    return 0;
}